# 🔐 GitHub - Размещение кода

## 1️⃣ Создание приватного репозитория

### Шаг 1: Создай репозиторий на GitHub

```
1. Зайди: https://github.com/new
2. Repository name: ai-telegram-agents
3. Description: AI Telegram боты для поиска в документах
4. ✅ Private (приватный!)
5. ❌ НЕ добавляй README, .gitignore, license
6. Нажми: Create repository
```

---

### Шаг 2: Подготовь код

```bash
cd ai-telegram-agents

# Создай .gitignore
cat > .gitignore << 'EOF'
# Секретные данные
.env
*.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/

# Логи
logs/
*.log

# PID файлы
.pids/
*.pid

# Загруженные файлы
uploads/
data/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
EOF
```

---

### Шаг 3: Инициализируй Git

```bash
# Инициализация
git init

# Добавь всё (кроме .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: AI Telegram Agents"

# Подключи GitHub
git remote add origin https://github.com/ТВО_ИМЯ/ai-telegram-agents.git

# Загрузи
git branch -M main
git push -u origin main
```

---

### Шаг 4: Проверь что .env НЕ загружен!

```bash
# Проверка
git ls-files | grep .env

# Должно быть ПУСТО!
# Если есть .env - удали из Git:
git rm --cached .env
git commit -m "Remove .env from tracking"
git push
```

---

## 2️⃣ Безопасность

### ✅ Что ДОЛЖНО быть в GitHub:

```
✅ Весь код (.py файлы)
✅ requirements.txt
✅ .env.example (БЕЗ реальных ключей!)
✅ Документация (.md файлы)
✅ Скрипты запуска (.sh, .bat)
✅ .gitignore
```

### ❌ Что НЕ ДОЛЖНО быть в GitHub:

```
❌ .env (с настоящими ключами!)
❌ Логи (logs/)
❌ Загруженные документы (data/)
❌ Временные файлы (__pycache__)
```

---

## 3️⃣ Работа с репозиторием

### Обновление кода:

```bash
# После изменений
git add .
git commit -m "Описание изменений"
git push
```

### Клонирование на сервер:

```bash
# На сервере
git clone https://github.com/ТВО_ИМЯ/ai-telegram-agents.git
cd ai-telegram-agents

# Создать .env вручную на сервере!
nano .env
# Скопировать настройки
```

---

## 4️⃣ Нужен ли GitHub заказчику?

### ❌ НЕТ! Заказчику GitHub НЕ нужен!

**Почему:**
- Код остаётся у тебя
- Заказчик работает только через веб-админку
- Не видит техническую часть

**Заказчик получает:**
- Ссылку на веб-админку: `https://твой-сервер.com:8000`
- Двух ботов в Telegram: `@ntd_helper_bot`, `@docs_sveta_helper_bot`
- Логин/пароль для админки (опционально)

---

## 5️⃣ Если нужен доступ заказчику

### Вариант 1: Дать доступ к репозиторию

```
1. Settings → Collaborators
2. Add people
3. Введи email заказчика
4. Выбери права: Read (только чтение)
```

### Вариант 2: Создать отдельный репозиторий для заказчика

```
Создай новый репозиторий:
- Название: ai-agents-client
- Содержимое: только документация
- Без кода!
```

---

## 6️⃣ Структура для продакшена

### Рекомендуемая структура:

```
ПРИВАТНЫЙ РЕПОЗИТОРИЙ (только ты):
github.com/твой-аккаунт/ai-telegram-agents
├── Весь код
├── .env.example
└── Документация

СЕРВЕР (разворачиваешь ты):
- IP: 123.45.67.89
- Работает: боты + админка
- Доступ: только у тебя (SSH)

ЗАКАЗЧИК получает:
- https://123.45.67.89:8000 (веб-админка)
- @ntd_helper_bot (Telegram)
- @docs_sveta_helper_bot (Telegram)
```

---

## 7️⃣ Обновление системы

### Когда нужно обновить код:

```bash
# На сервере
cd ai-telegram-agents
git pull  # Скачать обновления
./STOP.sh  # Остановить
./START.sh  # Запустить с новым кодом
```

**Заказчику ничего делать не нужно!**

---

## 🎯 Итого:

### GitHub:
- ✅ Приватный репозиторий
- ✅ Только ты имеешь доступ
- ✅ Заказчик НЕ видит код

### Заказчик:
- ✅ Веб-админка в браузере
- ✅ Боты в Telegram
- ✅ Не нужен GitHub
- ✅ Не нужен терминал

---

**GitHub нужен только ТЕБЕ для хранения кода!**

**Заказчик работает только через веб-интерфейс!**
