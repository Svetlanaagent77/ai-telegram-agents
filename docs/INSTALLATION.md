# Инструкция по установке и запуску

## 1. Получение необходимых API ключей

### 1.1 OpenAI API Key

1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в раздел API keys
3. Создайте новый ключ
4. Пополните баланс минимум на $5

**Стоимость:**
- Embeddings: ~$0.02 за 1M токенов
- GPT-4: ~$0.03 за 1K токенов

### 1.2 Pinecone API Key

1. Зарегистрируйтесь на https://www.pinecone.io/
2. В Dashboard создайте API key
3. Запишите также Environment (например, `us-east-1`)

**Free tier:** 100,000 векторов бесплатно

### 1.3 Создание Telegram ботов

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. Скопируйте токен бота

**Создайте 2 бота:**
- Один для НТД (нормативно-техническая документация)
- Один для Договоров

## 2. Установка проекта

### 2.1 Клонирование и настройка

```bash
cd ai-telegram-agents

# Создайте виртуальное окружение (опционально)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2.2 Конфигурация

```bash
# Скопируйте example в .env
cp .env.example .env

# Откройте .env и заполните:
nano .env
```

**Обязательные параметры в .env:**

```env
# Telegram
TELEGRAM_BOT_TOKEN_NTD=123456:ABC-DEF...
TELEGRAM_BOT_TOKEN_DOCS=789012:GHI-JKL...

# OpenAI
OPENAI_API_KEY=sk-proj-...

# Pinecone
PINECONE_API_KEY=ваш-ключ
PINECONE_ENVIRONMENT=us-east-1
PINECONE_INDEX_NTD=ntd-knowledge-base
PINECONE_INDEX_DOCS=docs-knowledge-base
```

## 3. Загрузка документов

### 3.1 Подготовка документов

Создайте директории:
```bash
mkdir -p data/ntd      # для ГОСТов, СНиПов
mkdir -p data/docs     # для договоров, документации
```

Скопируйте ваши PDF и DOCX файлы в соответствующие папки.

### 3.2 Загрузка в векторную базу

**Для НТД:**
```bash
python backend/utils/upload_documents.py --agent ntd --directory data/ntd
```

**Для Договоров:**
```bash
python backend/utils/upload_documents.py --agent docs --directory data/docs
```

**Загрузка одного файла:**
```bash
python backend/utils/upload_documents.py --agent ntd --file path/to/document.pdf
```

### 3.3 Проверка загрузки

Скрипт покажет:
- Сколько документов обработано
- Сколько чанков создано
- Статус загрузки в Pinecone

## 4. Запуск системы

### 4.1 Локальный запуск

```bash
python main.py
```

Вы увидите:
```
====================================================================
AI Telegram Agents - Запуск системы
====================================================================
✅ Конфигурация проверена
🔧 Инициализация RAG для Агента #1 (НТД)...
✅ RAG для НТД готов
🔧 Инициализация RAG для Агента #2 (Договоры)...
✅ RAG для Договоров готов
🤖 Создание Telegram ботов...
✅ Бот НТД создан
✅ Бот Договоры создан
🚀 Запуск ботов...
```

### 4.2 Тестирование

1. Найдите ваших ботов в Telegram
2. Отправьте `/start`
3. Задайте тестовый вопрос

**Примеры вопросов:**

Для бота НТД:
- "Какие требования по ГОСТ 12345?"
- "Что говорит СНиП о фундаментах?"

Для бота Договоры:
- "Какие условия оплаты в договоре №123?"
- "Найди информацию о сроках поставки"

## 5. Деплой на Railway.app (бесплатно)

### 5.1 Подготовка

1. Зарегистрируйтесь на https://railway.app/
2. Подключите GitHub аккаунт
3. Залейте код на GitHub

### 5.2 Создание проекта

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Логин
railway login

# Инициализация
railway init

# Деплой
railway up
```

### 5.3 Настройка переменных окружения

В Railway Dashboard добавьте все переменные из `.env`:
- TELEGRAM_BOT_TOKEN_NTD
- TELEGRAM_BOT_TOKEN_DOCS
- OPENAI_API_KEY
- PINECONE_API_KEY
- и т.д.

### 5.4 Автозапуск

Railway автоматически запустит `main.py` после деплоя.

## 6. Мониторинг и логи

### 6.1 Просмотр логов

**Локально:**
Логи выводятся в консоль

**На Railway:**
```bash
railway logs
```

Или в Dashboard → Deployments → Logs

### 6.2 Что отслеживать

- ✅ Успешные запросы
- ⚠️ Предупреждения (нет результатов)
- ❌ Ошибки (проблемы с API)

## 7. Обслуживание

### 7.1 Добавление новых документов

```bash
# Загрузите файл
python backend/utils/upload_documents.py --agent ntd --file новый_документ.pdf

# Перезапуска НЕ требуется
```

### 7.2 Обновление кода

```bash
git pull
pip install -r requirements.txt --upgrade
```

### 7.3 Мониторинг расходов

**OpenAI:**
https://platform.openai.com/usage

**Pinecone:**
Dashboard → Usage

## 8. Решение проблем

### Бот не отвечает

1. Проверьте логи: `railway logs`
2. Убедитесь, что токены правильные
3. Проверьте, что боты запущены: `ps aux | grep python`

### Не находит документы

1. Убедитесь, что документы загружены: `railway logs | grep "Загружено"`
2. Проверьте Pinecone Dashboard → Indexes
3. Попробуйте переформулировать вопрос

### Ошибка API ключей

```
ValueError: Отсутствуют обязательные переменные окружения
```

Решение: проверьте `.env` файл

### Медленная работа

1. Уменьшите `TOP_K_RESULTS` в config.py (с 3 до 2)
2. Используйте GPT-3.5 вместо GPT-4

## 9. Полезные команды

```bash
# Просмотр структуры проекта
tree -L 3

# Поиск в логах
grep "ERROR" logs.txt

# Остановка
Ctrl+C

# Проверка статуса на Railway
railway status

# Обновление на Railway
git push
railway up
```

## 10. Контакты и поддержка

При возникновении проблем:
1. Проверьте логи
2. Посмотрите документацию API (OpenAI, Pinecone)
3. Проверьте баланс на OpenAI

---

**Успешного запуска! 🚀**
