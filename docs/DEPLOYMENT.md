# Инструкция по развертыванию AI Telegram Agents

## Шаг 1: Получение API ключей

### 1.1. Pinecone (Векторная БД)

1. Зарегистрируйтесь на https://www.pinecone.io/
2. Создайте бесплатный аккаунт (Starter Plan - бесплатно)
3. Создайте новый индекс:
   - **Index Name:** `ntd-knowledge-base`
   - **Dimensions:** `1536` (для OpenAI embeddings)
   - **Metric:** `cosine`
   - **Pod Type:** `Starter` (бесплатно)
4. Создайте второй индекс:
   - **Index Name:** `docs-knowledge-base`
   - **Dimensions:** `1536`
   - **Metric:** `cosine`
5. Скопируйте:
   - API Key
   - Environment (например, `us-east1-gcp`)

### 1.2. OpenAI (AI модель и Embeddings)

1. Зарегистрируйтесь на https://platform.openai.com/
2. Пополните баланс минимум на $5-10
3. Создайте API ключ в разделе API Keys
4. Скопируйте ключ (начинается с `sk-...`)

**ИЛИ используйте Anthropic Claude:**

1. Зарегистрируйтесь на https://console.anthropic.com/
2. Создайте API ключ
3. В .env укажите `AI_PROVIDER=anthropic`

### 1.3. Telegram боты

1. Откройте Telegram и найдите @BotFather
2. Отправьте `/newbot`
3. Создайте первого бота:
   - Название: `НТД AI Ассистент`
   - Username: `your_ntd_bot`
   - Скопируйте токен
4. Создайте второго бота:
   - Название: `Договоры AI Ассистент`
   - Username: `your_docs_bot`
   - Скопируйте токен

## Шаг 2: Настройка проекта

### 2.1. Клонирование и установка зависимостей

```bash
cd ai-telegram-agents
pip install -r requirements.txt
```

### 2.2. Конфигурация .env

Создайте файл `.env` из примера:

```bash
cp .env.example .env
```

Заполните `.env` своими ключами:

```env
# Telegram Bot Tokens
TELEGRAM_BOT_TOKEN_NTD=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_TOKEN_DOCS=0987654321:ZYXwvuTSRqponMLKjihGFEdcba

# AI API Keys
OPENAI_API_KEY=sk-proj-...your-key...
# Или для Claude:
# ANTHROPIC_API_KEY=sk-ant-...your-key...

# AI Provider (openai или anthropic)
AI_PROVIDER=openai
AI_MODEL=gpt-4

# Pinecone
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_ENVIRONMENT=us-east1-gcp
PINECONE_INDEX_NTD=ntd-knowledge-base
PINECONE_INDEX_DOCS=docs-knowledge-base

# Database (для Railway)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Application
DEBUG=False
LOG_LEVEL=INFO
MAX_RESPONSE_TIME=10
```

## Шаг 3: Загрузка документов

### 3.1. Подготовка документов

Создайте папки:
```bash
mkdir -p documents/ntd
mkdir -p documents/docs
```

Поместите документы:
- `documents/ntd/` - ГОСТы, СНиПы, регламенты (PDF, DOCX)
- `documents/docs/` - Договоры, методички, письма (PDF, DOCX)

### 3.2. Загрузка в векторную БД

Для НТД:
```bash
python backend/scripts/load_documents.py --agent ntd --dir documents/ntd
```

Для Договоров:
```bash
python backend/scripts/load_documents.py --agent docs --dir documents/docs
```

Загрузка одного файла:
```bash
python backend/scripts/load_documents.py --agent ntd --file documents/ntd/gost-12345.pdf
```

## Шаг 4: Запуск ботов

### 4.1. Локальный запуск (для тестирования)

```bash
cd backend
python main.py
```

Вы должны увидеть:
```
✓ Конфигурация проверена
AI Provider: openai
✓ Pinecone подключен: ntd-knowledge-base
✓ Pinecone подключен: docs-knowledge-base
✓ OpenAI Embeddings подключен
✓ Telegram бот 'НТД' инициализирован
✓ Telegram бот 'Договоры' инициализирован
🚀 Запуск ботов...
Боты запущены!
```

### 4.2. Тестирование

1. Найдите своих ботов в Telegram
2. Отправьте `/start`
3. Задайте тестовый вопрос
4. Проверьте ответ

## Шаг 5: Развертывание на Railway (бесплатно)

### 5.1. Регистрация на Railway

1. Зайдите на https://railway.app/
2. Зарегистрируйтесь через GitHub
3. Бесплатный план: 500 часов/месяц, 512MB RAM

### 5.2. Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Подключите свой репозиторий
4. Railway автоматически обнаружит Python

### 5.3. Настройка переменных окружения

В Railway Dashboard → Variables:

Добавьте все переменные из `.env`:
- `TELEGRAM_BOT_TOKEN_NTD`
- `TELEGRAM_BOT_TOKEN_DOCS`
- `OPENAI_API_KEY`
- `PINECONE_API_KEY`
- и т.д.

### 5.4. Добавление PostgreSQL

1. В проекте нажмите "+ New"
2. Выберите "Database" → "PostgreSQL"
3. Railway автоматически создаст `DATABASE_URL`

### 5.5. Деплой

```bash
# Railway автоматически задеплоит при push в GitHub
git add .
git commit -m "Initial deployment"
git push
```

Railway автоматически:
- Установит зависимости из `requirements.txt`
- Запустит `python backend/main.py`
- Обеспечит 24/7 работу

## Шаг 6: Мониторинг

### 6.1. Логи на Railway

```
Railway Dashboard → Deployments → View Logs
```

### 6.2. Локальные логи

```bash
tail -f app.log
```

## Стоимость использования (примерная)

### Бесплатный tier:
- **Railway:** 500 часов/мес (бесплатно)
- **Pinecone:** 1 индекс, 100K векторов (бесплатно)
- **OpenAI:** 
  - Embeddings: ~$0.02 за 1000 документов
  - GPT-4: ~$0.03 за 1000 токенов (~100 ответов = $3)

### При 1000 запросов/месяц:
- Embeddings: ~$1-2
- GPT-4: ~$30-50
- **Итого: ~$35-55/месяц**

## Частые проблемы

### Ошибка: "Module not found"
```bash
pip install -r requirements.txt --upgrade
```

### Ошибка: "Pinecone connection failed"
Проверьте правильность `PINECONE_API_KEY` и `PINECONE_ENVIRONMENT`

### Бот не отвечает
1. Проверьте логи: `tail -f app.log`
2. Убедитесь что Railway деплой успешен
3. Проверьте баланс OpenAI

### Медленные ответы
1. Уменьшите `TOP_K_RESULTS` в config.py (с 3 до 2)
2. Используйте `gpt-3.5-turbo` вместо `gpt-4`

## Дальнейшее развитие

1. **Админ-панель** (Неделя 4 ТЗ)
2. **База данных** для хранения истории
3. **Аналитика** запросов
4. **Webhook** вместо long polling

## Поддержка

При возникновении проблем:
1. Проверьте логи: `app.log`
2. Проверьте `.env` файл
3. Убедитесь в наличии всех API ключей
4. Проверьте баланс на OpenAI
