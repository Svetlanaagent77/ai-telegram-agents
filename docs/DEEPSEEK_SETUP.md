# 🚀 Быстрый старт с DeepSeek API

## Почему DeepSeek?

### 💰 Невероятно дешево!

| Провайдер | Цена за 1M токенов | Разница |
|-----------|-------------------|---------|
| GPT-4 | ~$30 | - |
| GPT-3.5 | ~$2 | 15x дешевле GPT-4 |
| **DeepSeek** | **~$0.14** | **200x дешевле GPT-4!** |

**Для вашего проекта:**
- 1000 запросов/день = ~30,000 запросов/месяц
- С GPT-4: ~10,000₽/мес
- **С DeepSeek: ~50₽/мес** 😱

### ✅ Высокое качество

- Comparable to GPT-4 на многих задачах
- Отличная работа с русским языком
- OpenAI-совместимый API (легко переключиться)

---

## 📋 Пошаговая настройка

### Шаг 1: Получение DeepSeek API ключа

1. Перейдите на https://platform.deepseek.com/
2. Зарегистрируйтесь (можно через Google/GitHub)
3. Пополните баланс на $1-5 (этого хватит на месяцы!)
4. Создайте API ключ в разделе "API Keys"
5. Скопируйте ключ (формат: `sk-...`)

### Шаг 2: OpenAI API ключ (только для embeddings)

**ВАЖНО:** DeepSeek пока не поддерживает embeddings, поэтому нужен OpenAI ключ **только для embeddings**.

1. Зарегистрируйтесь на https://platform.openai.com/
2. Создайте API ключ
3. Пополните баланс на $1-2

**Стоимость embeddings:** ~$0.02 за 1M токенов (очень дешево!)

### Шаг 3: Pinecone (векторная БД)

1. Зарегистрируйтесь на https://www.pinecone.io/
2. Создайте API ключ
3. Запишите Environment (обычно `us-east-1`)

**Free tier:** 100,000 векторов бесплатно!

### Шаг 4: Telegram боты

1. Найдите @BotFather в Telegram
2. Создайте 2 бота командой `/newbot`
3. Скопируйте токены

---

## ⚙️ Конфигурация проекта

### 1. Создайте .env файл

```bash
cp .env.example .env
nano .env
```

### 2. Заполните .env файл

```env
# ========== Telegram ==========
TELEGRAM_BOT_TOKEN_NTD=123456:ABC-DEF...
TELEGRAM_BOT_TOKEN_DOCS=789012:GHI-JKL...

# ========== DeepSeek ==========
AI_PROVIDER=deepseek
AI_MODEL=deepseek-chat
DEEPSEEK_API_KEY=sk-ваш-deepseek-ключ
DEEPSEEK_BASE_URL=https://api.deepseek.com

# ========== OpenAI (только для embeddings) ==========
OPENAI_API_KEY=sk-proj-ваш-openai-ключ
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small

# ========== Pinecone ==========
PINECONE_API_KEY=ваш-pinecone-ключ
PINECONE_ENVIRONMENT=us-east-1
PINECONE_INDEX_NTD=ntd-knowledge-base
PINECONE_INDEX_DOCS=docs-knowledge-base
```

---

## 🚀 Запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Загрузка документов

```bash
# Создайте папки
mkdir -p data/ntd data/docs

# Скопируйте PDF/DOCX в папки

# Загрузите в векторную БД
python backend/utils/upload_documents.py --agent ntd --directory data/ntd
python backend/utils/upload_documents.py --agent docs --directory data/docs
```

### 3. Запуск ботов

```bash
python main.py
```

Вы увидите:
```
✅ Конфигурация проверена
✅ RAG для НТД готов (провайдер: deepseek)
✅ RAG для Договоров готов (провайдер: deepseek)
✅ Бот НТД создан
✅ Бот Договоры создан
🚀 Запуск ботов...
```

---

## 💡 Тестирование

### Telegram бот НТД:
```
Вы: Какие требования к материалам?
Бот: [ответ на основе документов из базы НТД]
```

### Telegram бот Договоры:
```
Вы: Какие сроки поставки в договоре?
Бот: [ответ на основе загруженных договоров]
```

---

## 📊 Мониторинг расходов

### DeepSeek Platform
https://platform.deepseek.com/usage

### OpenAI Platform (только embeddings)
https://platform.openai.com/usage

### Примерные расходы на месяц

**При 1000 запросов/день:**
- DeepSeek (генерация): ~50₽
- OpenAI (embeddings): ~10₽
- Pinecone: 0₽ (free tier)

**Итого: ~60₽/мес** 🎉

Сравните с GPT-4: ~10,000₽/мес

---

## 🔄 Переключение на GPT-4 (если нужно)

Просто измените в `.env`:

```env
AI_PROVIDER=openai
AI_MODEL=gpt-4
OPENAI_API_KEY=sk-proj-...
```

И перезапустите: `python main.py`

---

## ❓ FAQ

### Q: Можно ли использовать DeepSeek для embeddings?
**A:** Пока нет, используйте OpenAI (очень дешево: $0.02/1M токенов)

### Q: Качество ответов DeepSeek хуже GPT-4?
**A:** На простых задачах - практически идентично. На сложных - немного хуже, но разница в цене огромная!

### Q: Можно ли смешивать провайдеров?
**A:** Да! Можно использовать DeepSeek для одного бота, GPT-4 для другого.

### Q: Есть ли лимиты DeepSeek?
**A:** Да, но они очень высокие. Free tier: 1M токенов/день.

---

## 🎯 Рекомендуемая стратегия

1. **Начните с DeepSeek** - протестируйте систему
2. **Оцените качество** - достаточно ли для ваших задач?
3. **При необходимости** - переключитесь на GPT-4 для критичных запросов

**Гибридный вариант:**
- DeepSeek для Агента Договоры (простые запросы)
- GPT-4 для Агента НТД (сложные технические вопросы)

---

**Удачи! 🚀**
