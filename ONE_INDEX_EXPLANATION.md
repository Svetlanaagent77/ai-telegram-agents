# 🔄 Новая архитектура: 1 индекс вместо 2

## ✅ Что изменилось:

### БЫЛО (2 индекса):
```
Pinecone FREE tier проблема:
├── ❌ Индекс #1: "ntd-knowledge-base"      (База НТД)
├── ❌ Индекс #2: "docs-knowledge-base"     (База Договоры)
└── ❌ FREE tier = только 1 индекс!
```

### СТАЛО (1 индекс с фильтрами):
```
Pinecone FREE tier совместимо! ✅
└── ✅ Индекс: "ai-agents-knowledge-base"
    ├── Документы НТД (фильтр: agent_type='ntd')
    └── Документы Договоры (фильтр: agent_type='docs')
```

---

## 🎯 Как это работает:

### Загрузка документов:

**Для НТД:**
```python
# При загрузке добавляется метаданные
metadata = {
    'agent_type': 'ntd',  # ← Маркер НТД
    'filename': 'ГОСТ_12345.pdf',
    'doc_type': 'ГОСТ'
}
```

**Для Договоры:**
```python
metadata = {
    'agent_type': 'docs',  # ← Маркер Договоры
    'filename': 'Договор_123.pdf',
    'doc_type': 'Договор'
}
```

### Поиск с фильтрами:

**Агент НТД (@ntd_helper_bot):**
```python
# Ищет ТОЛЬКО документы с agent_type='ntd'
results = index.query(
    vector=query_embedding,
    filter={'agent_type': 'ntd'}  # ← Фильтр!
)
```

**Агент Договоры (@docs_sveta_helper_bot):**
```python
# Ищет ТОЛЬКО документы с agent_type='docs'
results = index.query(
    vector=query_embedding,
    filter={'agent_type': 'docs'}  # ← Фильтр!
)
```

---

## 🔐 Изоляция данных:

### ✅ Полная изоляция сохраняется!

```
Пользователь спрашивает @ntd_helper_bot:
"Какие требования по ГОСТ 12345?"
    ↓
Поиск с фильтром: agent_type='ntd'
    ↓
Находит ТОЛЬКО ГОСТы, СНиПы, ТУ
    ↓
НЕ видит договоры, письма, методички
    ↓
Ответ на основе НТД


Пользователь спрашивает @docs_sveta_helper_bot:
"Какие условия в договоре №123?"
    ↓
Поиск с фильтром: agent_type='docs'
    ↓
Находит ТОЛЬКО договоры, документы
    ↓
НЕ видит ГОСТы, СНиПы
    ↓
Ответ на основе документов
```

---

## 📊 Структура данных в Pinecone:

```
Индекс: ai-agents-knowledge-base
│
├── Вектор ID: "ntd_ГОСТ_12345_chunk_0"
│   ├── vector: [0.23, -0.15, ...]
│   └── metadata: {agent_type: 'ntd', filename: 'ГОСТ_12345.pdf'}
│
├── Вектор ID: "ntd_ГОСТ_12345_chunk_1"
│   ├── vector: [0.45, 0.12, ...]
│   └── metadata: {agent_type: 'ntd', filename: 'ГОСТ_12345.pdf'}
│
├── Вектор ID: "docs_Договор_123_chunk_0"
│   ├── vector: [-0.33, 0.78, ...]
│   └── metadata: {agent_type: 'docs', filename: 'Договор_123.pdf'}
│
└── Вектор ID: "docs_Договор_123_chunk_1"
    ├── vector: [0.91, -0.22, ...]
    └── metadata: {agent_type: 'docs', filename: 'Договор_123.pdf'}
```

---

## 💰 Экономия:

| Вариант | Стоимость | Индексы | Изоляция |
|---------|-----------|---------|----------|
| **1 индекс + фильтры** | **0₽/мес** | 1 | ✅ Полная |
| 2 индекса | ~7000₽/мес | 2 | ✅ Полная |

**Экономия: 100%!** 🎉

---

## 🔄 Что изменилось для пользователя:

### ❌ НИЧЕГО!

- ✅ Боты работают точно так же
- ✅ Изоляция данных сохраняется
- ✅ Веб-админка работает так же
- ✅ Команды загрузки те же

**Изменения только внутри кода!**

---

## 📝 Команды остались прежними:

### Загрузка через терминал:
```bash
# НТД
python backend/utils/upload_documents.py --agent ntd --directory data/ntd

# Договоры
python backend/utils/upload_documents.py --agent docs --directory data/docs
```

### Веб-админка:
```bash
python admin_panel.py
# Открой: http://localhost:8000
# Всё работает как раньше!
```

---

## 🔍 Проверка работы:

### После запуска `python main.py`:

```
✅ RAG для НТД готов (провайдер: deepseek)
   Индекс: ai-agents-knowledge-base
   Фильтр: agent_type='ntd'

✅ RAG для Договоров готов (провайдер: deepseek)
   Индекс: ai-agents-knowledge-base
   Фильтр: agent_type='docs'
```

### В Pinecone Dashboard:

Зайди на https://app.pinecone.io/

Увидишь **ОДИН** индекс:

```
📊 Indexes

┌─────────────────────────────────────┐
│ ai-agents-knowledge-base            │
│ Vectors: 579                        │  ← Все документы
│ Dimension: 1536                     │
│ Metric: cosine                      │
└─────────────────────────────────────┘

Внутри:
- 234 векторов с agent_type='ntd'
- 345 векторов с agent_type='docs'
```

---

## ✅ Преимущества новой архитектуры:

1. **Бесплатно навсегда** - FREE tier Pinecone
2. **Полная изоляция** - фильтры работают на уровне Pinecone
3. **Проще управление** - один индекс вместо двух
4. **Быстрее поиск** - Pinecone оптимизирует фильтры
5. **Легче масштабировать** - можно добавить 3-й, 4-й агент

---

## 🎯 Итого:

### Технически:
- 1 индекс в Pinecone
- Фильтры по метаданным
- Полная изоляция данных

### Для пользователя:
- Ничего не изменилось
- Всё работает как раньше
- Боты не видят чужие документы

### Для бюджета:
- 0₽ вместо 7000₽/мес
- FREE tier навсегда

---

**Всё работает! Изоляция сохранена! Бесплатно!** ✅

---

## 📚 Дополнительная информация:

### Как Pinecone фильтрует:

Pinecone использует **эффективные индексы метаданных**:
- Фильтрация происходит до поиска по векторам
- Не влияет на скорость
- Полная изоляция гарантирована

### Можно ли добавить 3-й агент?

**Да!** Просто добавь новый фильтр:
```python
agent_type='legal'  # Юридические документы
agent_type='technical'  # Техническая документация
agent_type='reports'  # Отчёты
# и т.д.
```

FREE tier = 100,000 векторов = достаточно для многих агентов!

---

**Теперь система работает на FREE tier Pinecone!** 🎉
